@model MiniTurboAz.Mvc.Models.CarViewModel?

@{
    ViewBag.Title = "Details";
    Layout = "_Layout";
}

<h2>Details</h2>

<br/>
<div class="container">
    <div class="row">
        <div class="col-md-4">
            <img src="@Model!.ImageUrl" class="img-fluid" alt="Responsive image">
        </div>
        <div class="col-md-8">
            <h3>@Model!.Name</h3>
            <p>@Model.Description</p>
        </div>
    </div>
</div>

<a href="@Url.Action("Index", "Home")">Back to list</a>